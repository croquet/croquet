{
  "name": "@croquet/2d",
  "version": "0.0.1",
  "description": "Simple 2D tests for Croquet",
  "scripts": {
    "start": "parcel --no-autoinstall src/*.html -p 9009",
    "build": "rimraf dist && parcel build --no-autoinstall --public-url ./ src/*.html"
  },
  "author": "",
  "license": "ISC",
  "browserslist": {
    "production": [
      "> 0.5%"
    ],
    "development": [
      "last 1 Chrome version",
      "last 1 Firefox version",
      "last 1 Safari version",
      "last 1 iOS major version"
    ]
  },
  "dependencies": {
    "@croquet/croquet": "file:../../packages/croquet"
  },
  "devDependencies": {
    "@parcel/transformer-image": "^2.14.4",
    "buffer": "^6.0.3",
    "crypto-browserify": "^3.12.1",
    "events": "^3.3.0",
    "parcel": "^2.14.4",
    "parcel-plugin-clean-dist": "0.0.6",
    "process": "^0.11.10",
    "rimraf": "^6.0.1",
    "stream-browserify": "^3.0.0",
    "string_decoder": "^1.3.0",
    "vm-browserify": "^1.1.2"
  }
}
